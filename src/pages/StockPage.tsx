import { Warehouse } from 'lucide-react';
import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function StockPage() {
  return (
    <PlaceholderPage
      title="Stock"
      icon={Warehouse}
      description="Gestão de stock de viaturas usadas. Funcionalidade a desenvolver."
    />
  );
}
