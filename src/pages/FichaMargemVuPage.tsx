import { Calculator } from 'lucide-react';
import { PlaceholderPage } from '@/components/PlaceholderPage';

/* Ficha de Margem (Vendas VU). Placeholder — o formato será definido de seguida. */
export default function FichaMargemVuPage() {
  return (
    <PlaceholderPage
      title="Ficha de Margem"
      icon={Calculator}
      description="Ficha de margem de Viaturas Usadas. Formato a definir."
    />
  );
}
